module.exports={
    // from:async (note,args,{models})=>{
    //     return await models.Note.findById(note.from);
    // },
    // to:async (note,args,{models})=>{
    //     return await models.Note.findById(note.to);
    // },
};